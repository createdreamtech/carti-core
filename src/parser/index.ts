import { parse } from "path";



/*

 parseLuaConfig(string config) => JSONObject validated by schema

gg

 parseLuaConfig(){

    const ast = parseLua(string)

    generateJSONfromLua(string) => obj
    keys = object.keys(ast)
    if()

    function toJSON(obj, node, []) {
        if(obj typeof obj)
            Object.keys(obj).



    }

 }
   {

   }
*/